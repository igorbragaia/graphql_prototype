type Task {
    id: Int,
    name: String,
    description: String,
    date: String,
    state: String,
    relateTag (
        tag_id: Int!
    ): Boolean,
    relateGoal (
        goal_id: Int!
    ): Boolean,
    user: User,
    tags: [Tag],
    goals: [Goal]
}